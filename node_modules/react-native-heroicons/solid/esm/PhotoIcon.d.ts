/// <reference types="react" />
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const PhotoIcon: ({ size, ...props }: Props) => JSX.Element;
export default PhotoIcon;
