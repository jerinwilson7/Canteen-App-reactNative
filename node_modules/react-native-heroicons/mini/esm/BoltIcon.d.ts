/// <reference types="react" />
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const BoltIcon: ({ size, ...props }: Props) => JSX.Element;
export default BoltIcon;
